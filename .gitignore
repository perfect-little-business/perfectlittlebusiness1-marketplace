# System noise
.DS_Store
Thumbs.db
desktop.ini

# Editor crud
.vscode/
.idea/
*.swp
*~

# Local archives / generated artifacts
*.zip
*.tar.gz

# Local logs
*.log

# Local Claude Code state (this repo ships a plugin; it is not itself a Claude project)
.claude/
.claude-cache/

# Internal-only reference material (workshop script, deploy procedure, etc.) —
# lives alongside the repo for convenience but never ships publicly.
_internal/

# Local session handoff — private working notes, never ships to clients.
NEXT-SESSION.md

# Skill test/eval workspaces — eval scratch plus personal brain content
# (interview transcripts, dossier drafts). Must never ship in the client
# plugin or be committed. Canonical deliverables live in _BRAIN.
perfectlittlebusiness/skills/*-workspace/
